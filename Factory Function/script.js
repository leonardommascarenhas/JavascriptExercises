const Gameboard = (() => {
  let gameBoard = ["", "", "", "", "", "", "", ""];
  return { gameBoard };
})();

const player = (name, marker) => {
  return {
    name,
    marker,
  };
};

const Game = (() => {})();
